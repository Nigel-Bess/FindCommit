<Window x:Class="FindCommit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FindCommit"
        mc:Ignorable="d"
        Title="Commit Search" Height="450" Width="800"
        d:DataContext="{d:DesignInstance  Type=local:MainWindowViewModel}">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="10"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding CurrentlyVisibleCommits}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Button Content="{Binding Message}" Command="{Binding DataContext.InvokeOpenCommitCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" HorizontalAlignment="Stretch" BorderThickness="0" HorizontalContentAlignment="Left">
                                    <Button.ToolTip>
                                        <ToolTip Content="{Binding Hash}" ToolTipService.InitialShowDelay="0"/>
                                    </Button.ToolTip>
                                <Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Copy commit hash"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                                </Button>
                            
   
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
        
       
    </Grid>
</Window>
